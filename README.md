# example-tanstack-start-auth

An example TanStack Start application demonstrating PKCE authentication with ZITADEL using `@zitadel/tanstack-start-auth`.

## Setup

```bash
cp .env.example .env
# Fill in your ZITADEL credentials in .env
npm install
npm run dev
```

## Environment Variables

See `.env.example` for required variables.
